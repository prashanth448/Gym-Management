# Optional override. In development the app defaults to http://localhost:5000/api.
# In production it defaults to /api.
REACT_APP_API_BASE_URL=

# Optional override for the real-time socket server. In development it defaults to http://localhost:5000.
# In production it defaults to the current site origin.
REACT_APP_REALTIME_URL=
